---
// Hero section for The Secretest Santa
---

<section id="hero" class="hero">
  <div class="container text-center">
    <h1 class="hero-title fade-in">The Secretest Santa</h1>
    <p class="hero-subtitle fade-in" style="animation-delay: 0.2s">
      A mathematically bulletproof way to exchange gifts
    </p>
    <p class="hero-description fade-in" style="animation-delay: 0.4s">
      Inspired by Tom 7's brilliant cryptographic solution to Secret Santa
    </p>
    <div class="hero-cta fade-in" style="animation-delay: 0.6s">
      <a href="#app" class="btn-primary" role="button">Create Your Secret Santa</a>
      <a href="#story" class="btn-secondary" role="button">Learn How It Works</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  .hero-title {
    font-size: clamp(3rem, 6vw, 5rem);
    margin-bottom: 1rem;
    color: var(--color-burgundy);
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
  }

  .hero-subtitle {
    font-size: clamp(1.2rem, 2.5vw, 1.8rem);
    color: var(--color-pine-green);
    margin-bottom: 0.5rem;
    font-weight: 400;
  }

  .hero-description {
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    color: var(--color);
    opacity: 0.8;
    margin-bottom: 2rem;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary,
  .btn-secondary {
    padding: 1rem 2rem;
    font-size: 1.1rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 600;
    transition: var(--transition-smooth);
    display: inline-block;
  }

  .btn-primary {
    background: var(--color-burgundy);
    color: white;
    border: 2px solid var(--color-burgundy);
  }

  .btn-primary:hover {
    background: var(--primary-hover);
    transform: scale(1.05);
    box-shadow: 0 8px 16px rgba(128, 0, 32, 0.3);
  }

  .btn-secondary {
    background: transparent;
    color: var(--color-pine-green);
    border: 2px solid var(--color-pine-green);
  }

  .btn-secondary:hover {
    background: var(--color-pine-green);
    color: white;
    transform: scale(1.05);
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 70vh;
      padding: 2rem 0;
    }

    .hero-cta {
      flex-direction: column;
      align-items: center;
    }

    .btn-primary,
    .btn-secondary {
      width: 100%;
      max-width: 300px;
    }
  }
</style>
